<UserControl
	x:Class="Itemcore.Client.Controls.RecentSolutions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    d:DesignHeight="300" 
	d:DesignWidth="300">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="60"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<Label Grid.Row="0" Content="Recently Opened Solutions" Height="Auto" Width="Auto" Margin="50,20,50,0" FontSize="18"/>
		<ListBox Name="RecentSolutionsListBox" Grid.Row="1" Height="Auto" Width="Auto" Padding="50,0,50,50" Margin="0,0,0,0" BorderBrush="Transparent">
			<ListBox.Resources>
				<BitmapImage x:Key="SolutionIcon" UriSource="/Itemcore.Client;component/Resources/Images/itemcore24x24.png" />
			</ListBox.Resources>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<Image Source="{StaticResource SolutionIcon}"/>
						<TextBlock ToolTip="{Binding Path}" Padding="10" Text="{Binding SolutionName}" FontSize="18" />
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
